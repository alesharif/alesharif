# ذاكرة بيانات Binance المؤقتة (كبيرة، تُعاد بناؤها من المرايا العامة)
backtest/cache/
*.parquet

# مخرجات قابلة لإعادة التوليد (تُضاف النتائج النهائية يدوياً عبر git add -f)
backtest/output/

# بايثون
__pycache__/
*.pyc
.venv/
venv/

# GA volatile run logs/checkpoints (النتائج النهائية تُحفظ في ga_best.json)
backtest/output/*.log
backtest/output/ga_checkpoint.json
